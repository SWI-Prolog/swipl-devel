# REL 		--- Relation Classes

!include ..\makefile.def

RELOBJS=	constraint.obj hyper.obj identity.obj relation.obj &
		spatial.obj

objects:	$(RELOBJS)
		@%null

clean:
		rm -f *~ a.out core $(RELOBJS) addlib