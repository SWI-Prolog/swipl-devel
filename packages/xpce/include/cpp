#!/bin/csh -f

set gccp=/`gcc -v |& head -1 | sed 's@[^/]*/\(.*\)/specs$@\1@'`/cpp
set m4prog=`dirname $0`/pce-cpp.m4
set argn=`expr $#argv - 1`
set input=$argv[$argn]

onintr clean
m4 $m4prog -s $input > $input.m4
set argv[$argn] = $input.m4
$gccp $*
clean:
rm $input.m4
