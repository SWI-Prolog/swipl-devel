* Print an error if, while expanding ns:local, ns is not known.

* Provide a meta-declaration for predicates accepting namespaces:

  :- rdf_meta
	rdf(:,:,:).
	...

* Add default_ns(Name) to rdf_save/2.

* Add indexing on source, to speedup saving a particular source as well as
  rdf_retractall(_,_,_,Src).

