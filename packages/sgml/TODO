TODO LIST:

	* Handle multi-valued attributes
	* Handling of external entities (both param and normal) in Prolog
	* Improve error messages on incomplete documents
	* Check ID/IDREF
	* Handle memory-allocation errors
	* Parsing of processing instructions to end at ?> instead of just >

XML NameSpaces

	* Allow for mapping to local identifiers?

		- xmlns(rdf, 'djfejfpwe').

XML Space handling

	* Recognise xml:space (default|preserve) attribute.  Default:

	 	- Canonise multiple to a single
		- Remove leading and trailing space of an element:

		  <a> foo <a>              --> 'foo'
		  <a> foo <b> x </b> </a>  --> 'foo ', b(x)
