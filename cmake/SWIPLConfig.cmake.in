@PACKAGE_INIT@

# Version information
set(SWIPL_VERSION_COUNT 3)
set(SWIPL_VERSION_MAJOR @SWIPL_VERSION_MAJOR@)
set(SWIPL_VERSION_MINOR @SWIPL_VERSION_MINOR@)
set(SWIPL_VERSION_PATCH @SWIPL_VERSION_PATCH@)
set(SWIPL_VERSION_STRING @SWIPL_VERSION_STRING@)

# Location suitable for passing to PL_initialise() as argv[0]
set(SWIPL_EXECUTABLE "@PACKAGE_SWIPL_INSTALL_ARCH_EXE@/@SWIPL_PROGRAM@")

# Exported targets
include("${CMAKE_CURRENT_LIST_DIR}/SWIPLTargets.cmake")
